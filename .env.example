# Public backend origin only. No /api/v1 suffix; contract paths include it.
EXPO_PUBLIC_API_URL=https://events-circle-api-production.up.railway.app
# Never add DATABASE_URL, signing keys or bucket credentials here.
